export interface goodsElemType {
  category: string;
  name: string;
  brand: string;
  structure: string;
  popularity: boolean;
  weight: string;
  length: string;
  price: number;
  url: string;
}

export interface categoriesElemType {
  category: string;
  url: string;
}
